import { MainCarousel, MainList } from "../../components/main";
import { Main } from "../../common";

function MainPage() {
	return (
		<Main>
			<MainList />
		</Main>
	);
}

export default MainPage;
